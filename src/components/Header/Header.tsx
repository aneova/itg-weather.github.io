import React from 'react';
import { HeaderContainer, Title } from './styed';

const Header: React.FC = () => {  
  return (
    <HeaderContainer>
      <Title>React App Weather</Title>  
    </HeaderContainer>
  );
};

export default Header;
